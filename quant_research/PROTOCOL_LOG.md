# 协议版本台账

> 裁判（`quant-backtest-protocol`）**可以被修**，但每一版都要在这里留下记录。
> `qr protocol` 用它把当前指纹翻译成版本号，`qr rejudge` 用它算出哪些历史裁决需要重判。
>
> **只追加，不改写既有行。** 指纹是内容哈希，回答不了「这条裁决在新版下还成不成立」；
> 版本号加上「影响」才回答得了。这是「裁判不许动」被换掉的那条铁律的替代物。

## 影响取值

| 影响 | 含义 | 谁要重判 |
|---|---|---|
| `baseline` | 起始版本 | 无 |
| `none` | 只改文档、注释、重构，判读结果不可能变 | 无 |
| `tighten` | 门槛变严：原来能过的可能不再过 | 全部 `PASS` 卡 |
| `loosen` | 门槛变松：原来被拒的可能能过 | 全部 `FAIL` / `PARK` 卡 |
| `breaking` | 口径改变，方向不单一 | 全部已裁决卡 |

登记时选最保守的那一档。拿不准是 `tighten` 还是 `breaking`，就写 `breaking`。

## 版本

| 版本 | 指纹 | 登记时间 | 影响 | 说明 |
|---|---|---|---|---|
| v1 | `49d889c195be79ff` | 2026-09-03T00:00:00Z | `baseline` | 台账起始版本：schema v2 严格报告、A 层成本闸、角色门禁、指纹盖章 |
| v2 | `d0bf25c7af2c0ee7` | 2026-09-05T02:40:00Z | `tighten` | 验收门禁改为 fail-closed：置换/归因/搜索调整/路径统计一律要求有限实数且在合法区间，NaN、±inf、越界值、布尔与字符串不再被放行；外部置换结果保留原始精度，0.94999 不再被四舍五入成 0.95 后晋级；qr 写入 PASS 前独立复核同一批统计值。证据与复现见 protocol_issues/2026-09-05-gpt-nonfinite-permutation/ |
| v3 | `02a1d5457de17265` | 2026-09-05T05:10:50Z | `tighten` | randomport 的 --n-short 不再有静默默认值 5：纯多策略必须显式写 --n-short 0，否则拿多空中性随机组合当零假设会把置换分位系统性抬高（实测同一策略 75.05%→99.65%）；alpha_beta 的结论句改为带显著性判断，不显著的正 alpha 不再被写成「策略本身有效」（B0002） |
| v4 | `75a867c1e95745a3` | 2026-09-05T05:20:28Z | `none` | 新增 qbt.py carrier：执行角色的载体替换验收（同暴露 beta 容差 + 暴露调整后差额双门禁 + 压力成本档 + 跨期一致 + 回撤不更深 + 载体风险逐项申报）；qr 的 PASS 校验按角色分支，execution 走 carrier 门禁不再套 alpha 的判读线（B0004）。不改动任何既有 alpha 判读线，历史裁决不受影响 |
| v5 | `f186e8ce5874ee75` | 2026-09-05T05:24:03Z | `none` | B0001 前两项：兜底统计校验挪进严格分支，--exploratory 的诊断报告不再混入「random_portfolio/selection_adjustment 缺失」噪音；置换别名一致性改为四位小数比较（原 abs_tol=1e-12 会误杀「一个字段存4位一个存6位」的合法文件），判读仍用未取整原值。只读扫描 68 份含置换别名的 JSON、5 份多别名文件，无一被原容差误杀，故历史裁决不受影响 |
| v6 | `ee6d8b8f80506038` | 2026-09-05T12:58:42Z | `tighten` | 新增对照同构性门禁 control_shape_check：真策略对宇宙等权的 beta 必须落在随机对照的 beta 分布附近（默认容差 0.35），否则置换分位不予采信、直接判 FAIL。纯多策略配多空中性对照时 beta 差约 1.0，正是 B0002 那个把分位从 75.05% 抬到 99.65% 的事故。随机对照的输出新增 universe_size 与 null_beta_p05/p50/p95。配套回归测试本身就是反例（B0007 的要求：给不出反例的检查等于没有检查） |
