# gpt 长跑续接：按协议缺陷停止

## 为什么停

验收程序能把无效统计当作通过。已用人工数据对原版工具完成最小端到端复现，触发 `PROMPT_LONGRUN.md` 停止条件第 5 条。没有推进新策略、修改协议或把测试结果写入策略台账。

证据与修复建议：`protocol_issues/2026-09-05-gpt-nonfinite-permutation/README.md`。

## 跑到哪了

本次长跑已完成四轮（不含此前同日其他研究记录），三红一黄，未发现可晋级策略：

1. H0031 PARK：挂单版本缺可验证排队与成交的历史数据，需补前瞻真实事件。
2. H0032 FAIL：国债拍卖后的收益没有证明比简单承担债券风险更好。
3. H0034 FAIL：月末效应强度不足；这是本五轮的中分校准位。
4. H0035 FAIL：给小盘加质量过滤，换到后期更差。
5. H0036 仅选题占坑：60/40 股债波动率管理。未完成成本闸、预注册或回测；因协议缺陷释放，仍未裁决。

下一轮原计划研究 H0036，理由是跨资产低波动方向的覆盖空白和清晰的正反文献。现在必须先由人处理验收缺陷，不可自动重开研究。

## 收口与边界

- 保护目录及 qr 未修改；未交易、未部署、未提交 git。
- 人工复现文件单独放在 protocol_issues，醒目标记不是市场研究证据。
- 没有把缺陷误记成 H0036 的策略失败；使用 release 保留卡片供后人查重。
- 第五轮未完成，不能宣称已完成五轮研究。前三次 FAIL 死因不同，不触发连续八轮同死因条件。
- 验收工具违反 quant-backtest-protocol 的无效输入不得通过硬约束，本轮只报告，不擅自修复。

## 最终自检

- H0036 已释放，状态 abandoned、裁决仍为 OPEN，不代表策略失败。
- 释放后 `qr audit`：cards=41，errors=0，warnings=0；legacy_v1=29，未过成本闸=30 为全局历史计数。
- `qr protocol`：49d889c195be79ff，与本次长跑开工一致。
- 保护目录、PROMPT、qr 与 templates 的 git status 输出为空；研究复现代码被现有 gitignore 忽略，未加入 git。

## 2026-09-05T02:20:39Z 再次启动核验（gpt）

- 本次重新读取长跑说明、母提示词、协议技能与台账，完成 leads/coverage/scorecard/list/stale 盘点；未新认领假设，本次新增裁决为零。
- 既有停止条件仍成立：复核现行 qbt.py 第 336–350、836–837 行，置换结果仍未检查有限性与合法范围。脚本 SHA256 仍为 `b86f6ef5041ba1ac10be2a9224d5f034945372b0fa4699caa20f5628a6bedec3`，与既有人工复现完全一致；本次读取既有证据，未重复生成市场或人工收益。
- 开工及停止核验的协议指纹均为 `49d889c195be79ff`；最新 audit 为 cards=43、errors=0、warnings=0。保护目录、PROMPT、qr、templates 无 git 改动。
- 按停止条件第 5 条结束本次循环。H0036 继续保持已释放、未裁决；H0043 为 claude 的活跃占坑，不干预。下一步仍需先由人安排协议修复与旧报告影响范围检查，再恢复策略研究。

## 2026-09-05 用户授权协议维护后：停止原因已修复

- 用户后续明确要求处理问题，本次修复 qbt 外部置换输入、最终裁决及 qr 写入前的数值校验，未改成本或验收阈值。
- 11 项测试通过，原始漏洞复现全部被拒绝；只读扫描 35 份历史报告，未发现严格 PASS 报告，完整扫描边界见修复记录。
- 新协议指纹 `d0bf25c7af2c0ee7`，旧指纹 `49d889c195be79ff` 保留在历史卡片中。此为明确授权维护产生的版本迁移，后续长跑以新指纹为开工基线。
- 这一项停止原因解除；本次只完成维护，没有认领或裁决新策略，H0036 仍未完成研究。详见 `protocol_issues/2026-09-05-gpt-nonfinite-permutation/repair/README.md`。

## 修复后重新开工

新基线 d0bf25c7af2c0ee7。候选池与七维评分：[续跑候选池](2026-09-05-gpt-restart/candidate_pool.md)。已授权修复的三处工作区差异作为开工基线，研究过程不得继续改；记录文件内容hash，保护历史卡片。当前仅认领H0044（parent H0021）。

## H0044 收口与累计五轮自检

H0044：FAIL／尾部不可接受。保险后官方指数MDD29.2%，无保险57.42%；只作A风险证伪，不当可交易收益。累计前五轮 H0031/H0032/H0034/H0035/H0044：红4、黄1、绿0；H0036释放不计一轮。
1. 死因没有集中：数据不可得、信号无增量、强度不足、样本外翻转、尾部不可接受各异，无八连同死因。
2. 本轮不是为产出凑FAIL：检验父卡明确提出的保险重开条件，获得“保险有效但单周期上限不约束跨期回撤”的可复用区别。
3. 历史高分/中分均无PASS，当前证据不能证明评分能区分可用策略；不改七维与权重，下个五轮继续留中分校准。下一优先级为H0013贵金属线索，优先查可成交窗口与成本，不把连续合约拼接当真实收益。

## 2026-09-05T12:58:38Z 本次长跑启动：开工体检未通过

- 身份：gpt。已读取 PROMPT_LONGRUN.md 与 PROMPT.md，执行 `QR_AGENT=gpt ./qr start --agent gpt`，退出码 1；按规定停止在阶段 0，未认领新卡、未新增裁决，本次完成 0 轮。
- 体检：47 张卡，audit errors=0、warnings=0。阻止启动的是尚未关闭的 HALT：B0003（H0047 候选待人工复核）、B0005（腾讯加法复权数据口径修复未完成）。已阅读两条阻塞的完整处理日志，未重复开阻塞或代为关闭。
- B0003 后续日志明确撤回头条收益数字的可用性，须先解决 B0005，再重跑 H0047；不能把最早的 PASS 报告视为当前有效候选证据。B0005 已有根因调查与重建建议，但状态仍为 open，原因查明不等于数据修复、现金流核验与历史重判完成。
- 开工输出当时把协议 ee6d8b8f80506038 标为未登记；随后只读 `qr protocol` 已显示 v6。共享状态存在时点变化，不据此擅自登记版本，也不据此忽略仍存在的 HALT。
- 本次基线见 runs/gpt.env。受保护文件已有工作区变更不归因于本次研究；本次未编辑协议、提示词、qr、模板或历史裁决。
- 下一步：由人安排 B0005 的数据维护与影响卡重判，然后复核 H0047 并按台账流程处理 B0003；相关 HALT 正式关闭、重新 start 通过后，再从 leads/coverage/scorecard/KNOWLEDGE 盘点恢复逐卡研究。
