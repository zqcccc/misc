# workbuddy 长跑终期盘点 · 2026-09-08

> run token: `workbuddy-1788859040-13981`（`./qr start --agent workbuddy` 铸）
> 状态：**已停**。原因 = 探索空间收敛，见 §3。

## 0. 一句话结论

连续 3 轮（H0106/H0107/H0108）全部 FAIL，但收口处证明了一件事：**台账 108 张卡里，
所有「方向类」策略（动量/反转/择时/信号/事件）已被系统性证伪，仅存的两条活路都是
「结构性摩擦」（载体替换 H0047、横截面 value H0076），且一条不可复制、一条已确认是局部最优。
低成本可测的探索空间已经耗尽，剩下的要么高成本（逐券信用研究）、要么条件触发、要么按构造为空。

## 1. 本轮做了哪几轮

| 卡 | 市场 | 结论 | 死因 | 一句话人话 |
|---|---|---|---|---|
| H0106 | cn_convertible | FAIL | 成本吃穿 | 转债隔夜涨日内跌是真的，但每天进出一次，手续费比赚的还多，日内做空又没券 |
| H0107 | cn_commodity | FAIL | 信号无增量 | 商品期货追涨杀跌已死：毛收益≈躺着买全部的一半，剥掉后倒贴 2 点，2021 后转负 |
| H0108 | cn_convertible | FAIL | 信号无增量 | 低价和低溢价是同一维度（都映射正股 β），「双低」只是加 β 不是加 alpha |

（H0106/H0107 的完整过程见 `.workbuddy/memory/2026-09-08.md` 与各自假设卡。）

## 2. 本轮最有价值的增量：H0108 的两个副产品

1. **数据层打通**：`akshare.bond_zh_cov_value_analysis` 返回历史转股价值/转股溢价率（PIT，
   下修日单日跳升=用时变转股价）。这是 H0076 因「转股价历史不可 PIT」而放弃双低口径的解法。
   已落盘 `cn_convertible/data/panel_conv_value.parquet` + `panel_premium.parquet`（953 只、0 缺失、
   1564 次下修跳升、溢价率缺失占比 0.002%）。**后人做「高溢价剔除」「双低陷阱」可直接复用，不用再拉数据。**
2. **确认 H0076 是局部最优**：价格/溢价率的任何线性组合（交集、经典双低值、纯低溢价）都在
   调 β 不调 alpha。H0076 那条「仅低价」边扛住了增强攻击，往这个方向继续堆没有意义。
   想更纯只能往「信用质量/回售条款/到期期限」这些真正独立的债性维度走，但那需要逐券信用研究（高成本）。

## 3. 为什么停（探索空间收敛）

三个维度同时收敛：

1. **`qr leads` 只剩 5 条，且全是指向条件触发**（funding 回升、TEST 段质量 alpha 转正、
   拿到特殊回购融资现券报价、新增 10 年月度事件）。没有一条是「现在就能低成本上手」的。
2. **`qr coverage` 的空白格都在高成本/空构造象限**：`ml`（需特征工程+低可解释性，台账已有 1 例 FAIL）、
   `crypto_spot`/`crypto_perp` 的 value/quality（加密资产无基本面，按构造为空）、
   `cn_stock` reversal/seasonality（H0061 已判「A股日线横截面 2017 后不可开采」，先验偏负）。
3. **死因高度集中**：`信号无增量 / 成本吃穿 / 方向反了 / 强度不足 / 样本外翻转` 占了绝大多数，
   而唯一 PASS（H0047/H0076）的共同点是**结构性/制度摩擦**，不是信息预测。这个模式已经足够清晰，
   再跑只会重测死方向，违反台账「命中签名即跳过」的铁律。

**元结论（值得人记住的一句）**：在这些流动性好、定价相对有效的市场里（A股/港股/美股/商品/fx/crypto），
「预测方向」这条路基本走不通；唯一持续存在的边，来自**做空限制、评级门槛这类制度失衡造成的定价扭曲**。
下一阶段该把研究资源从「找新 alpha」转移到「把已证实的结构性边推进实盘 + 找第三个结构性失衡」。

## 4. 留给下一阶段的建议（按性价比排序）

1. **推 H0076 落 PASS 进 paper trading**（B0021 ASK 阻塞还挂着等人拍板）。这是唯一证实的 alpha 边，
   比再挖一条新边价值更高。注意其三条黄旗：赢在风险调整后、post-2024 t=1.11 站不住、单日 −4.76% 距预注册线仅 0.24pp。
2. **沿「结构性摩擦」主轴找第三个失衡**（低成本的候选：A股打新/战略配售、ETF 申赎套利、可转债下修博弈的信用维度）。
   这是 H0047（载体替换）+ H0076（低价折价）之后的自然延伸。
3. **校准位 / 方法论改进**：B0018 指出台账里「效应在后段消失」的判断大多没报 MDE；B0016 指出 `qr search`
   索引不到 §5/§7 里「算过但没命名」的切分。这两条值得在下一阶段开工前先修，避免白占坑。

## 5. 收工自检

`QR_RUN=workbuddy-1788859040-13981 ./qr selfcheck --agent workbuddy` → **EXIT=0（通过）**。
未改动任何受保护文件（协议本体 / PROMPT / qr / 已裁决卡的 §7§8）。
