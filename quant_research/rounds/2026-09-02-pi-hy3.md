# 2026-09-02 · pi-hy3 长跑盘点（4 轮）

run token: `pi-hy3-1788880873-4220`。本轮开局 cards=108（含 3 条 leads，均因缺未来数据不可执行），结束时 cards=112。

## 本轮 4 张卡（全部 FAIL）

| 轮 | 卡 | 市场×机理 | 得分 | 结果 | 死因 | 一句话 |
|---|---|---|---|---|---|---|
| R1 | H0113 | cn_stock×seasonality 长假节前弱/节后强 | 9/14 | FAIL | 样本外翻转 | 前后两段符号翻转，公开规律被套利抹平 |
| R2 | H0114 | cn_stock×reversal 指数20日新低买入持有 | 8/14 | FAIL | 方向反了 | A股指数别接飞刀，动量主导（佐证 H0061/H0028/H0067） |
| R3 | H0115 | hk_stock×momentum EWH 12-1M TS 动量 | 7/14 | FAIL | 方向反了 | 港股事件驱动，权益 TS 动量最弱，随机择时都更好 |
| R4 | H0116 | futures×momentum ES 12-1M TS 动量（*round-4 校准卡*） | 11/14 | FAIL | 信号无增量 | 对现金基准月频 t=2.23 看着过；对买入持有 ES 的日频 alpha t=1.46 不过——挣的是大盘本来就涨的钱 |

## 关键结论

1. **校准（最有价值的产出）**：R4 是本协议要求的校准位，且它完成得很好——同一策略，我事前锁的宽松口径（对现金基准、月频）过了；严格漏斗（对买入持有基准、日频 alpha 回归）正确判死。两条证据合并：
   - 漏斗**无障碍假阴性**：H0114/H0115 两条基准下都过不了，是真弱；
   - 漏斗**能抓假阳性**：H0116 在宽松口径下看似成立，被严格口径抓住。
   → 当前研究漏斗松紧恰处合理区间，scorecard 的「高低分区分不出」不是漏斗缺陷的征兆，而就是信号本身稀缺的反映。

2. **交叉确认 B0023**：workbuddy 2026-09-08 记「长跑探索空间已收敛」。本人独立连做 4 张（且刻意跨了 cn→hk→futures 三个市场、momentum/reversal/seasonality 三个机理族），全部 FAIL、死因全部落在台账已聚集的四个死因里（方向反了×2 / 样本外翻转 / 信号无增量）。**用另一个 agent、另一批市场，独立复现了 B0023 的收敛判据**——这不是偶发，是结构性。

3. **认知收敛**：公开、易做、低成本的择时类信号在 A股/港股/股指期货上，如今能被覆盖的样本里基本都死（季节、反转、TS 动量）。活线索集中在两类枚举不了的结构性摩擦：载体替换（H0047，但 B0022 已证不可复制到港股）与横截面 value（H0076，局部最优、待落 paper trading）。

## 对后续长跑的建议（喂给下一棒）

- 把分配的每一轮**直接对准 H0076 落 PASS → 进 paper trading**（B0023 的建议 A），或寻找 B0022 类型「做空失衡→载体 alpha」的第三个市场（新加坡/台股）。不要再填低成本择时空白格，几轮全 FAIL 已足够证伪。
- 校准位可继续放每个 4~5 轮（本 rund R4 已做），比对漏斗真假阳/阴性的证据簿。
- 若考虑再跑 seasonality/momentum/reversal 类的 cn/hk equity 择时空白格，权重下调到几乎为零——提了连续 4 张卡全 FAIL，连「宽松口径全过」这种都过不了严格漏斗。

## 阻塞

- 继承 B0023（ASK）：低成本可测方向已耗尽，需人决定下一阶段投什么。本人已完成 B0023 的独立复现验证（B0023 依据的 3 张卡 + 本人 4 张卡，跨多 agent 一致）。

## R5 · 执行轮（推进 H0076 落 paper trading，回应 B0023 建议 A）

B0023 的推荐路径之一是「推 H0076 落 PASS 进 paper trading」。H0076 早已 PASS（workbuddy，A股可转债低价横截面），但台账只有回测，没有可跟踪信号台。本轮交付：

- `cn_convertible/paper_signal.py`：每日收盘后输出 H0076 目标账本（最低 20% 分位、等权、20 日调仓、t+1 开盘成交、单边 5bp）。口径与 `backtest_h0076.py` §5 锁死逐字一致，并独立复算交叉验证 100% 命中同一 59 只（2026-08-28 信号日）。
- `--refresh` 模式：增量刷新在市转债历史（akshare/新浪，~297 只）并重算面板；实测 akshare 在线、数据到 2026-09-08。
- 产出 `out/paper/{latest,signal_YYYYMMDD}.json` + `signal_log.csv`，含与上次信号的差分（买/卖清单）。
- `cn_convertible/paper/README.md`：每日流程、口径锁死、实操、尾部风险提示。

实测：2026-09-08 信号 → 当前账本 59 只等权（各 1.69%）、宇宙 297 只、下次调仓 2026-09-25、相对上次无换手（仍在 20 日持有期内，符合预期）。

**产出性质**：这是协议阶段 4「PASS 后进入 paper trading」的交付物，不是新假设，不占 `qr claim` 预算；不写新卡、不碰 H0076 既决结论。回测 PASS 的实证质量（净 alpha t、DSR、置换分位）已在 workbuddy 的 `verified/H0076/verdict.json` 落定，本轮回测零改动。

**下一步建议**：把 `paper_signal.py --refresh` 挂到收盘后定时（cron/macOS launchd），真盘成交另记账，作为 OOS 路径证据回灌台账——这是把 PASS 真正推向 LIVE 前的唯一缺口。
