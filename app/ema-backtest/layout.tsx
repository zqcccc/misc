export const metadata = {
  title: '实验性回测工具',
  robots: { index: false, follow: false },
}

export default function EmaBacktestLayout({ children }: { children: React.ReactNode }) {
  return children
}
